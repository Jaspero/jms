import { BlockLinkDirective } from './block-link.directive';

describe('BlockLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockLinkDirective();
    expect(directive).toBeTruthy();
  });
});
