import { FolderIconPipe } from './folder-icon.pipe';

describe('FolderIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FolderIconPipe();
    expect(pipe).toBeTruthy();
  });
});
